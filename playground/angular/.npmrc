# @storybook/angular@10.4.2 peers @angular-devkit/architect "<0.2200.0", but this
# playground runs Angular 22 (architect 0.2200.0). The combination installs and
# builds correctly; only npm's strict peer check objects, so relax it here.
legacy-peer-deps=true
