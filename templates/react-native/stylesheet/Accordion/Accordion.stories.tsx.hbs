// Crucible preview examples for {{name}} (React Native / StyleSheet).
// ponytail: Storybook for React Native isn't wired up in this release (needs Expo +
// @storybook/react-native) — these are ready-to-spread prop presets for manual/visual QA
// (e.g. `<{{name}} {...{{name}}Previews.default}>…</{{name}}>`) — not a Storybook CSF file.
{{#if features.compoundComponents}}
import type { AccordionRootProps, AccordionSize } from './{{name}}';

// ponytail: compound mode — presets configure <{{name}}.Root>; supply Item/Trigger/Content children.
export const {{name}}Previews: Record<string, AccordionRootProps & { size?: AccordionSize }> = {
{{else}}
import type { AccordionProps } from './{{name}}';

// ponytail: flat mode — pass an `items` array alongside any of these presets.
export const {{name}}Previews: Record<string, AccordionProps> = {
{{/if}}
{{#each variants}}  {{this}}: {},
{{/each}}{{#each sizes}}  {{this}}Size: { size: '{{this}}' },
{{/each}}  singleOpen: { defaultValue: 'overview' },
  multipleOpen: { type: 'multiple', defaultValue: ['shipping', 'billing'] },
  nonCollapsible: { collapsible: false, defaultValue: 'terms' },
};
