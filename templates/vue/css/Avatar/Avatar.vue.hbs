<script setup lang="ts">
import { ref, computed } from 'vue';

export type AvatarVariant = {{> shared/variant-types}};
export type AvatarSize    = {{> shared/size-types}};

interface Props {
  src?:      string;
  alt?:      string;
  fallback?: string;
  variant?:  AvatarVariant;
  size?:     AvatarSize;
}

const props = withDefaults(defineProps<Props>(), {
  src: '',
  alt: '',
  fallback: '',
  variant: 'circle',
  size: 'md',
});

const errored = ref(false);
const showImage = computed(() => Boolean(props.src) && !errored.value);
const initials = computed(() => (props.fallback || props.alt || '').trim().slice(0, 2).toUpperCase());
</script>

<template>
  <div
    :class="['avatar', `avatar--${variant}`, `avatar--${size}`]"
    role="img"
    :aria-label="alt || fallback || undefined"
  >
    <img
      v-if="showImage"
      class="avatar-image"
      :src="src"
      :alt="alt"
      @error="errored = true"
    />
    <span v-else class="avatar-fallback" aria-hidden="true">\{{ initials }}</span>
  </div>
</template>

<style scoped>
/* Generated by Crucible — edit freely, re-generate with --force */

.avatar {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  overflow: hidden;
  background-color: var(--avatar-fallback-bg, {{tokens.componentTokens.avatar.fallbackBg}});
  color: var(--avatar-fallback-color, {{tokens.componentTokens.avatar.fallbackColor}});
  font-family: var(--font-family, system-ui, -apple-system, sans-serif);
  font-weight: var(--avatar-font-weight, {{tokens.componentTokens.avatar.fontWeight}});
  line-height: 1;
  user-select: none;
}

.avatar--circle {
  border-radius: 9999px;
}

.avatar--square {
  border-radius: var(--avatar-radius, {{tokens.componentTokens.avatar.radius}});
}

.avatar--xs {
  width: 24px;
  height: 24px;
  font-size: 0.625rem;
}

.avatar--sm {
  width: 32px;
  height: 32px;
  font-size: 0.75rem;
}

.avatar--md {
  width: 40px;
  height: 40px;
  font-size: 0.875rem;
}

.avatar--lg {
  width: 56px;
  height: 56px;
  font-size: 1.125rem;
}

.avatar-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.avatar-fallback {
  line-height: 1;
}
</style>
