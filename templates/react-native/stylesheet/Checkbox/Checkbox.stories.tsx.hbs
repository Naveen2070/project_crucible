// Crucible preview examples for {{name}} (React Native / StyleSheet).
// ponytail: Storybook for React Native isn't wired up in this release (needs Expo +
// @storybook/react-native) — these are ready-to-spread prop presets for manual/visual QA
// (e.g. `<Checkbox {...{{name}}Previews.default} />`), not a Storybook CSF file.
import type { CheckboxProps } from './{{name}}';

export const {{name}}Previews: Record<string, CheckboxProps> = {
{{#each variants}}  {{this}}: { label: '{{capitalize this}} checkbox', variant: '{{this}}'{{#if (eq this "error")}}, error: 'You must accept the terms'{{/if}} },
{{/each}}{{#each sizes}}  {{this}}Size: { label: '{{capitalize this}} checkbox', size: '{{this}}' },
{{/each}}  checked: { label: 'Email notifications', checked: true },
  indeterminate: { label: 'Select all', indeterminate: true },
  disabled: { label: 'Archived item', disabled: true },
};
