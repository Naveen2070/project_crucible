// Crucible preview examples for {{name}} (React Native / NativeWind).
// ponytail: Storybook for React Native isn't wired up in this release (needs Expo +
// @storybook/react-native) — these are ready-to-spread prop presets for manual/visual QA
// (e.g. `<Input {...{{name}}Previews.default} />`), not a Storybook CSF file.
import type { InputProps } from './{{name}}';

export const {{name}}Previews: Record<string, InputProps> = {
{{#each variants}}  {{this}}: { label: '{{capitalize this}} variant', variant: '{{this}}'{{#if (eq this "error")}}, error: 'This field is required'{{/if}} },
{{/each}}{{#each sizes}}  {{this}}Size: { label: '{{capitalize this}} size', size: '{{this}}' },
{{/each}}  withHint: { label: 'Password', hint: 'Must be at least 8 characters long', secureTextEntry: true },
  disabled: { label: 'Account ID', editable: false, value: 'ACC-12345' },
  required: { label: 'Full Name', required: true, placeholder: 'Jane Doe' },
};
