# {{name}}

Generated by Crucible v{{engineVersion}} | Theme: {{theme}} | Framework: {{framework}}

## Variants
{{#each variants}}
- `{{{this}}}`{{/each}}

## Sizes
{{#each sizes}}
- `{{{this}}}`{{/each}}

## States
{{#each states}}
- `{{{this}}}`{{/each}}

{{#if (eq framework "react-native")}}
## Design Tokens

{{#if (eq styleSystem "stylesheet")}}
This component reads its tokens from the generated `theme.ts` (a plain JS object — see
`{{themeImportPath}}`). Re-run `crucible tokens --force` (or `crucible add ... --force`) after
changing your config to refresh it.
{{else}}
This component's classNames resolve against the generated `tailwind.preset.js` — add
`presets: [require('./tailwind.preset.js')]` to your `tailwind.config.js`. Re-run
`crucible tokens --force` (or `crucible add ... --force`) after changing your config to refresh it.
{{/if}}
{{else}}
## CSS Variables Used

```css
:root {
{{#each tokens.cssVars}}  {{@key}}: {{this}};
{{/each}}
}
```

{{#if tokens.darkCssVars}}
## Dark Mode CSS Variables

{{#if (eq tokens.darkModeStrategy 'manual')}}
```css
.dark {
{{#each tokens.darkCssVars}}  {{@key}}: {{{this}}};
{{/each}}
}
```
{{else}}
```css
@media (prefers-color-scheme: dark) {
  :root {
{{#each tokens.darkCssVars}}    {{@key}}: {{this}};
{{/each}}  }
}
```
{{/if}}
{{/if}}
{{/if}}

---

_This file is auto-generated. Edit the source template or run `crucible upgrade` after updating your config._
