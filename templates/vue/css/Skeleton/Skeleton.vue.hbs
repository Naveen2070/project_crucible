<script setup lang="ts">
import { computed } from 'vue';

export type SkeletonVariant = {{> shared/variant-types}};

interface Props {
  variant?:  SkeletonVariant;
  width?:    string | number;
  height?:   string | number;
  animated?: boolean;
}

const props = withDefaults(defineProps<Props>(), {
  variant: 'default',
  width: undefined,
  height: undefined,
  animated: true,
});

const sizeStyle = computed(() => ({
  width: typeof props.width === 'number' ? `${props.width}px` : props.width,
  height: typeof props.height === 'number' ? `${props.height}px` : props.height,
}));
</script>

<template>
  <div
    :class="['skeleton', `skeleton--${variant}`, { 'skeleton--animated': animated }]"
    :style="sizeStyle"
    aria-busy="true"
    aria-live="polite"
  />
</template>

<style scoped>
/* Generated by Crucible — edit freely, re-generate with --force */

.skeleton {
  display: block;
  width: 100%;
  height: 1rem;
  background-color: var(--skeleton-base-color, {{tokens.componentTokens.skeleton.baseColor}});
  border-radius: var(--skeleton-radius, {{tokens.componentTokens.skeleton.radius}});
}

.skeleton--text {
  height: 0.8em;
  border-radius: var(--radius-sm, 4px);
}

.skeleton--circle {
  width: 2.5rem;
  height: 2.5rem;
  border-radius: 9999px;
}

.skeleton--rect {
  border-radius: 0;
}

.skeleton--animated {
  animation: skeleton-pulse var(--skeleton-duration, {{tokens.componentTokens.skeleton.duration}}) ease-in-out infinite;
}

@keyframes skeleton-pulse {
  0%,
  100% {
    opacity: 1;
  }
  50% {
    opacity: 0.4;
  }
}

@media (prefers-reduced-motion: reduce) {
  .skeleton--animated {
    animation: none;
  }
}
</style>
