const generatedId = `{{prefix}}-\${Math.floor(Math.random() * 1000000)}`;
const {{prefix}}Id = computed(() => props.id || generatedId);
const {{suffix}}Id = computed(() => `\${{{prefix}}Id.value}-{{suffix}}`);
