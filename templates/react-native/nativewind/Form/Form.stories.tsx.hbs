// Crucible preview examples for {{name}} (React Native / NativeWind).
// ponytail: Storybook for React Native isn't wired up in this release (needs Expo +
// @storybook/react-native) — these are ready-to-spread prop presets for manual/visual QA
// (e.g. `<Form {...{{name}}Previews.default} />`), not a Storybook CSF file.
import type { FormRootProps, FormSubmitProps } from './{{name}}';

/** Stable no-op handler so preset objects stay referentially stable across renders. */
const noop = () => {};

export const {{name}}Previews: Record<string, FormRootProps> = {
{{#each variants}}  {{this}}: { variant: '{{this}}' },
{{/each}}{{#each sizes}}  {{this}}Size: { size: '{{this}}' },
{{/each}}  login: { defaultValues: { email: '', password: '' }, onSubmit: noop, onError: noop },
  disabled: { disabled: true },
};

export const {{name}}SubmitPreviews: Record<string, FormSubmitProps> = {
  idle: { label: 'Save changes' },
  disabled: { label: 'Save changes', disabled: true },
  submitting: { label: 'Saving…', disabled: true },
  disableOnInvalid: { label: 'Continue', disableOnInvalid: true },
};
