{{> shared/components/card-base}}

.title {
  margin:      0;
  padding:     calc(var(--spacing-unit) * 4);
  font-size:   1.1em;
  font-weight: 600;
  border-bottom: 1px solid var(--color-border);
}

.content {
  padding: calc(var(--spacing-unit) * 4);
}
