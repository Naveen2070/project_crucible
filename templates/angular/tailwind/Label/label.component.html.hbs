<label [class]="getTailwindClasses()" [attr.for]="htmlFor || null">
  <ng-content></ng-content>
  @if (required) {
    <span class="ml-0.5 text-[var(--label-required-color,var(--color-destructive))]" aria-hidden="true">*</span>
  }
</label>
