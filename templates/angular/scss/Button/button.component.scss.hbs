/* Generated by Crucible — edit freely, re-generate with --force */

:root {
{{#each tokens.cssVars}}  {{@key}}: {{{this}}};
{{/each}}
}

{{#if tokens.darkCssVars}}
{{#if (eq tokens.darkModeStrategy 'manual')}}
.dark {
{{#each tokens.darkCssVars}}  {{@key}}: {{{this}}};
{{/each}}
}
{{else}}
@media (prefers-color-scheme: dark) {
{{#each tokens.darkCssVars}}  {{@key}}: {{{this}}};
{{/each}}
}
{{/if}}
{{/if}}

{{!--
  Uses the shared button-base partial (same source as the css template) so the full,
  data-driven variant/size set stays in sync. The drift guard
  (src/__tests__/templates/css-scss-parity.test.ts) enforces this parity.
--}}
{{> shared/components/button-base}}
