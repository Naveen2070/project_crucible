// Crucible preview examples for {{name}} (React Native / NativeWind).
// ponytail: Storybook for React Native isn't wired up in this release (needs Expo +
// @storybook/react-native) — these are ready-to-spread prop presets for manual/visual QA
// (e.g. `<Progress {...{{name}}Previews.half} />`), not a Storybook CSF file.
import type { ProgressProps } from './{{name}}';

export const {{name}}Previews: Record<string, ProgressProps> = {
{{#each variants}}  {{this}}: { variant: '{{this}}', value: 60 },
{{/each}}{{#each sizes}}  {{this}}Size: { size: '{{this}}', value: 60 },
{{/each}}  empty: { value: 0 },
  full: { value: 100 },
  overMax: { value: 140 },
  indeterminate: { variant: 'linear', indeterminate: true },
};
