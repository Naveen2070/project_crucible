import type { Meta, StoryObj } from '@storybook/react';
import React, { useState } from 'react';
import { Dialog } from './Dialog';
import { Button } from '../Button/Button';

const meta: Meta<typeof Dialog> = {
  title: 'Crucible/Dialog',
  component: Dialog,
  parameters: { layout: 'centered' },
  argTypes: {
    variant:  { control: 'select', options: {{> shared/variant-options}} },
    size:     { control: 'select', options: {{> shared/size-options}} },
  },
};
export default meta;
type Story = StoryObj<typeof Dialog>;

{{#if features.compoundComponents}}
export const Default: Story = {
  args: { variant: 'default', size: 'md' },
  render: (args) => {
    const [isOpen, setIsOpen] = useState(false);
    return (
      <div style=\{{ height: '300px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <button onClick={() => setIsOpen(true)}>Open Dialog</button>
        <Dialog {...args} isOpen={isOpen} onClose={() => setIsOpen(false)}>
          <Dialog.Overlay>
            <Dialog.Content>
              <Dialog.Header>
                <Dialog.Title>Dialog Title</Dialog.Title>
                <Dialog.Close />
              </Dialog.Header>
              <Dialog.Body>
                <p>This is the Dialog body content.</p>
              </Dialog.Body>
              <Dialog.Footer>
                <div style=\{{ display: 'flex', gap: '8px', justifyContent: 'flex-end' }}>
                  <button onClick={() => setIsOpen(false)}>Cancel</button>
                  <button onClick={() => setIsOpen(false)}>Confirm</button>
                </div>
              </Dialog.Footer>
            </Dialog.Content>
          </Dialog.Overlay>
        </Dialog>
      </div>
    );
  },
};

{{#each variants}}
{{#unless (eq this "default")}}
export const {{capitalize this}}: Story = {
  args: { variant: '{{this}}' },
  render: (args) => {
    const [isOpen, setIsOpen] = useState(false);
    return (
      <div style=\{{ height: '300px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <button onClick={() => setIsOpen(true)}>Open {{capitalize this}} Dialog</button>
        <Dialog {...args} isOpen={isOpen} onClose={() => setIsOpen(false)}>
          <Dialog.Overlay>
            <Dialog.Content>
              <Dialog.Header>
                <Dialog.Title>{{capitalize this}} Variant</Dialog.Title>
                <Dialog.Close />
              </Dialog.Header>
              <Dialog.Body>
                <p>Dialog content with {{this}} variant.</p>
              </Dialog.Body>
            </Dialog.Content>
          </Dialog.Overlay>
        </Dialog>
      </div>
    );
  },
};
{{/unless}}
{{/each}}

{{#each sizes}}
export const {{capitalize this}}: Story = {
  args: { size: '{{this}}' },
  render: (args) => {
    const [isOpen, setIsOpen] = useState(false);
    return (
      <div style=\{{ height: '300px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <button onClick={() => setIsOpen(true)}>Open {{capitalize this}} Dialog</button>
        <Dialog {...args} isOpen={isOpen} onClose={() => setIsOpen(false)}>
          <Dialog.Overlay>
            <Dialog.Content>
              <Dialog.Header>
                <Dialog.Title>{{capitalize this}} Size</Dialog.Title>
                <Dialog.Close />
              </Dialog.Header>
              <Dialog.Body>
                <p>Dialog content with {{this}} size.</p>
              </Dialog.Body>
            </Dialog.Content>
          </Dialog.Overlay>
        </Dialog>
      </div>
    );
  },
};
{{/each}}
{{else}}
export const Default: Story = {
  args: { variant: 'default', size: 'md', title: 'Dialog Title' },
  render: (args) => {
    const [isOpen, setIsOpen] = useState(false);
    return (
      <div style=\{{ height: '300px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <button onClick={() => setIsOpen(true)}>Open Dialog</button>
        <Dialog
          {...args}
          isOpen={isOpen}
          onClose={() => setIsOpen(false)}
        >
          <p>This is the Dialog content.</p>
        </Dialog>
      </div>
    );
  },
};

{{#each variants}}
{{#unless (eq this "default")}}
export const {{capitalize this}}: Story = {
  args: { variant: '{{this}}', title: '{{capitalize this}} Variant' },
  render: (args) => {
    const [isOpen, setIsOpen] = useState(false);
    return (
      <div style=\{{ height: '300px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <button onClick={() => setIsOpen(true)}>Open {{capitalize this}} Dialog</button>
        <Dialog
          {...args}
          isOpen={isOpen}
          onClose={() => setIsOpen(false)}
        >
          <p>Dialog content with {{this}} variant.</p>
        </Dialog>
      </div>
    );
  },
};
{{/unless}}
{{/each}}

{{#each sizes}}
export const {{capitalize this}}: Story = {
  args: { size: '{{this}}', title: '{{capitalize this}} Size' },
  render: (args) => {
    const [isOpen, setIsOpen] = useState(false);
    return (
      <div style=\{{ height: '300px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <button onClick={() => setIsOpen(true)}>Open {{capitalize this}} Dialog</button>
        <Dialog
          {...args}
          isOpen={isOpen}
          onClose={() => setIsOpen(false)}
        >
          <p>Dialog content with {{this}} size.</p>
        </Dialog>
      </div>
    );
  },
};
{{/each}}
{{/if}}
