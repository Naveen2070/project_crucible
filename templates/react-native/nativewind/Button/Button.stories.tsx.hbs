// Crucible preview examples for {{name}} (React Native / NativeWind).
// ponytail: Storybook for React Native isn't wired up in this release (needs Expo +
// @storybook/react-native) — these are ready-to-spread prop presets for manual/visual QA
// (e.g. `<Button {...{{name}}Previews.primary}>Button</Button>`), not a Storybook CSF file.
import type { ButtonProps } from './{{name}}';

export const {{name}}Previews: Record<string, Omit<ButtonProps, 'children'>> = {
{{#each variants}}  {{this}}: { variant: '{{this}}' },
{{/each}}{{#each sizes}}  {{this}}Size: { variant: 'primary', size: '{{this}}' },
{{/each}}  loading: { variant: 'primary', loading: true },
  disabled: { variant: 'primary', disabled: true },
};
