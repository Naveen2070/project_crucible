// Crucible preview examples for {{name}} (React Native / NativeWind).
// ponytail: Storybook for React Native isn't wired up in this release (needs Expo +
// @storybook/react-native) — these are ready-to-spread prop presets for manual/visual QA
// (e.g. `<Avatar {...{{name}}Previews.square}>JD</Avatar>`), not a Storybook CSF file.
import type { AvatarProps } from './{{name}}';

export const {{name}}Previews: Record<string, AvatarProps> = {
{{#each variants}}  {{this}}: { variant: '{{this}}', alt: 'Jane Doe' },
{{/each}}{{#each sizes}}  {{this}}Size: { size: '{{this}}', alt: 'Jane Doe' },
{{/each}}  image: { src: 'https://i.pravatar.cc/150?u=jane', alt: 'Jane Doe' },
  erroredImage: { src: 'https://invalid.invalid/jane.png', alt: 'Jane Doe' },
};
