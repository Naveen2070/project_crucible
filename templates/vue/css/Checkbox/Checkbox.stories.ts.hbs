import type { Meta, StoryObj } from '@storybook/vue3';
import { ref } from 'vue';
import Checkbox from './Checkbox.vue';

const meta: Meta<typeof Checkbox> = {
  title: 'Crucible/{{name}}',
  component: Checkbox,
  tags: ['autodocs'],
  argTypes: {
    variant:       { control: 'select', options: {{> shared/variant-options}} },
    size:          { control: 'select', options: {{> shared/size-options}} },
    indeterminate: { control: 'boolean' },
    disabled:      { control: 'boolean' },
  },
};

export default meta;
type Story = StoryObj<typeof Checkbox>;

export const Default: Story = {
  args: { label: 'Accept terms and conditions', size: 'md' },
  render: (args) => ({
    components: { Checkbox },
    setup() {
      const value = ref(false);
      return { args, value };
    },
    template: `<Checkbox v-bind="args" v-model="value" />`,
  }),
};

export const Checked: Story = {
  args: { label: 'Subscribe to newsletter' },
  render: (args) => ({
    components: { Checkbox },
    setup() {
      const value = ref(true);
      return { args, value };
    },
    template: `<Checkbox v-bind="args" v-model="value" />`,
  }),
};

export const Indeterminate: Story = {
  args: { label: 'Select all', indeterminate: true },
  render: (args) => ({
    components: { Checkbox },
    setup() {
      const value = ref(false);
      return { args, value };
    },
    template: `<Checkbox v-bind="args" v-model="value" />`,
  }),
};

export const WithError: Story = {
  args: { label: 'Required option', variant: 'error', error: 'You must accept to continue' },
  render: (args) => ({
    components: { Checkbox },
    setup() {
      const value = ref(false);
      return { args, value };
    },
    template: `<Checkbox v-bind="args" v-model="value" />`,
  }),
};
