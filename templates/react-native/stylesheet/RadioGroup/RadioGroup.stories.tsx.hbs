// Crucible preview examples for {{name}} (React Native / StyleSheet).
// ponytail: Storybook for React Native isn't wired up in this release (needs Expo +
// @storybook/react-native) — these are ready-to-spread prop presets for manual/visual QA
// (e.g. `<{{name}} {...{{name}}Previews.default}>{items…}</{{name}}>`), not a Storybook CSF file.
import type { RadioGroupProps } from './{{name}}';

export const {{name}}Previews: Record<string, RadioGroupProps> = {
{{#each variants}}  {{this}}: {},
{{/each}}{{#each sizes}}  {{this}}Size: { size: '{{this}}', name: 'contact-method' },
{{/each}}  default: { name: 'contact-method', defaultValue: 'email' },
  horizontal: { orientation: 'horizontal', name: 'contact-method', defaultValue: 'phone' },
  locked: { value: 'email', name: 'locked-choice' },
};
