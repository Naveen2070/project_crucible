import type { Meta, StoryObj } from '@storybook/angular';
import { componentWrapperDecorator } from '@storybook/angular';
import { PopoverComponent } from './popover.component';

const meta: Meta<PopoverComponent> = {
  title: 'Crucible/Popover',
  component: PopoverComponent,
  tags: ['autodocs'],
  argTypes: {
    placement: {
      control: 'select',
      options: [
        'top', 'top-start', 'top-end',
        'bottom', 'bottom-start', 'bottom-end',
        'left', 'left-start', 'left-end',
        'right', 'right-start', 'right-end'
      ],
    },
    trigger: {
      control: 'select',
      options: ['click', 'hover'],
    },
    variant: {
      control: 'select',
      options: ['default', 'minimal'],
    },
    size: {
      control: 'select',
      options: ['sm', 'md', 'lg'],
    },
    showArrow: {
      control: 'boolean',
    },
  },
  decorators: [
    componentWrapperDecorator((story) => `<div style="height: 300px; display: flex; align-items: center; justify-content: center;">${story}</div>`),
  ],
};

export default meta;
type Story = StoryObj<PopoverComponent>;

export const Default: Story = {
  args: {
    placement: 'bottom',
    showArrow: true,
  },
  render: (args) => ({
    props: args,
    template: `
      <app-popover [placement]="placement" [trigger]="trigger" [variant]="variant" [size]="size" [showArrow]="showArrow">
        {{#if features.compoundComponents}}
        <button popover-trigger type="button" class="btn btn--primary">Open Popover</button>
        <div popover-content style="display: flex; flex-direction: column; gap: 8px;">
          <h3 style="margin: 0; font-size: 1rem;">Popover Title</h3>
          <p style="margin: 0; font-size: 0.875rem; color: var(--color-text-muted);">
            This is a rebuilt, portal-ready Angular popover.
          </p>
          <button (click)="args.isOpen = false" class="btn btn--minimal btn--sm">Close</button>
        </div>
        {{else}}
        <button trigger type="button">Open Popover</button>
        <div style="display: flex; flex-direction: column; gap: 8px;">
          <h3 style="margin: 0; font-size: 1rem;">Popover Title</h3>
          <p style="margin: 0; font-size: 0.875rem; color: var(--color-text-muted);">
            This is a rebuilt Angular popover using the default slot.
          </p>
        </div>
        {{/if}}
      </app-popover>
    `,
  }),
};

export const WithItems: Story = {
  render: (args) => ({
    props: args,
    template: `
      <app-popover [placement]="placement" [showArrow]="showArrow">
        {{#if features.compoundComponents}}
        <button popover-trigger type="button" class="btn">User Menu</button>
        <div popover-content style="padding: 4px; min-width: 160px; display: flex; flex-direction: column;">
          <div style="padding: 8px 12px; font-size: 0.75rem; font-weight: bold; color: var(--color-text-muted);">Account</div>
          <div style="padding: 8px 12px; cursor: pointer;">Profile</div>
          <div style="padding: 8px 12px; cursor: pointer;">Settings</div>
          <div style="height: 1px; background: var(--color-border); margin: 4px 0;"></div>
          <div style="padding: 8px 12px; cursor: pointer; color: var(--color-error);">Logout</div>
        </div>
        {{else}}
        <button trigger type="button">User Menu</button>
        <div style="padding: 4px; min-width: 160px; display: flex; flex-direction: column;">
          <div style="padding: 8px 12px; font-size: 0.75rem; font-weight: bold; color: var(--color-text-muted);">Account</div>
          <div style="padding: 8px 12px; cursor: pointer;">Profile</div>
        </div>
        {{/if}}
      </app-popover>
    `,
  }),
};

export const Placements: Story = {
  decorators: [
    componentWrapperDecorator((story) => `<div style="height: 400px; display: grid; grid-template-columns: repeat(2, 1fr); gap: 20px; align-items: center; justify-items: center;">${story}</div>`),
  ],
  render: () => ({
    template: `
      <app-popover placement="top">
        <button trigger type="button">Top</button>
        <div style="padding: 10px;">Top Content</div>
      </app-popover>
      <app-popover placement="bottom">
        <button trigger type="button">Bottom</button>
        <div style="padding: 10px;">Bottom Content</div>
      </app-popover>
      <app-popover placement="left">
        <button trigger type="button">Left</button>
        <div style="padding: 10px;">Left Content</div>
      </app-popover>
      <app-popover placement="right">
        <button trigger type="button">Right</button>
        <div style="padding: 10px;">Right Content</div>
      </app-popover>
    `,
  }),
};

