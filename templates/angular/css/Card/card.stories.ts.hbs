import { Meta, StoryObj } from '@storybook/angular';
import { CardComponent } from './card.component';

const meta: Meta<CardComponent> = {
  title: 'Crucible/Card',
  component: CardComponent,
  tags: ['autodocs'],
  argTypes: {
    variant: { control: 'select', options: ['default', 'hoverable', 'clickable'] },
    title:   { control: 'text' },
  },
};

export default meta;
type Story = StoryObj<CardComponent>;

export const Default: Story = {
  args: {
    title: 'Card Title',
    variant: 'default',
  },
  render: (args) => ({
    props: args,
    template: `<app-card [title]="title" [variant]="variant">This is the card content.</app-card>`,
  }),
};

export const Hoverable: Story = {
  args: {
    title: 'Hoverable Card',
    variant: 'hoverable',
  },
  render: (args) => ({
    props: args,
    template: `<app-card [title]="title" [variant]="variant">Hover over me!</app-card>`,
  }),
};

export const Clickable: Story = {
  args: {
    title: 'Clickable Card',
    variant: 'clickable',
  },
  render: (args) => ({
    props: args,
    template: `<app-card [title]="title" [variant]="variant">Click me!</app-card>`,
  }),
};
