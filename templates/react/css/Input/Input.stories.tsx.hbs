{{> react/stories-header}}
  argTypes: {
    variant:  { control: 'select', options: {{> shared/variant-options}} },
    size:     { control: 'select', options: {{> shared/size-options}} },
    disabled: { control: 'boolean' },
    required: { control: 'boolean' },
  },
{{> react/stories-footer}}

{{#if features.compoundComponents}}
export const Default: Story = {
  args: { variant: 'default', size: 'md' },
  render: (args) => (
    <Input {...args}>
      <Input.Label>Username</Input.Label>
      <Input.Field placeholder="Enter your username" />
    </Input>
  ),
};

{{#each variants}}
{{#unless (eq this "default")}}
export const {{capitalize this}}: Story = {
  args: { variant: '{{this}}' },
  render: (args) => (
    <Input {...args}>
      <Input.Label>{{capitalize this}} Variant</Input.Label>
      <Input.Field placeholder="Input with {{this}} variant" />
    </Input>
  ),
};
{{/unless}}
{{/each}}

{{#each sizes}}
export const {{capitalize this}}: Story = {
  args: { size: '{{this}}' },
  render: (args) => (
    <Input {...args}>
      <Input.Label>{{capitalize this}} Size</Input.Label>
      <Input.Field placeholder="Input with {{this}} size" />
    </Input>
  ),
};
{{/each}}

export const WithError: Story = {
  args: { error: 'Invalid email address' },
  render: (args) => (
    <Input {...args}>
      <Input.Label>Email</Input.Label>
      <Input.Field defaultValue="invalid-email" />
      <Input.Error>{args.error}</Input.Error>
    </Input>
  ),
};

export const WithHint: Story = {
  args: { hint: 'Must be at least 8 characters long' },
  render: (args) => (
    <Input {...args}>
      <Input.Label>Password</Input.Label>
      <Input.Field type="password" placeholder="Enter password" />
      <Input.Hint>{args.hint}</Input.Hint>
    </Input>
  ),
};

export const Disabled: Story = {
  args: { disabled: true },
  render: (args) => (
    <Input {...args}>
      <Input.Label>Account ID</Input.Label>
      <Input.Field value="ACC-12345" readOnly />
    </Input>
  ),
};

export const Required: Story = {
  args: { required: true },
  render: (args) => (
    <Input {...args}>
      <Input.Label>Full Name</Input.Label>
      <Input.Field placeholder="Jane Doe" />
    </Input>
  ),
};
{{else}}
export const Default: Story = {
  args: { label: 'Username', placeholder: 'Enter your username', size: 'md' },
};

{{#each variants}}
{{#unless (eq this "default")}}
export const {{capitalize this}}: Story = {
  args: { label: '{{capitalize this}} Variant', variant: '{{this}}', placeholder: 'Input with {{this}} variant' },
};
{{/unless}}
{{/each}}

{{#each sizes}}
export const {{capitalize this}}: Story = {
  args: { label: '{{capitalize this}} Size', size: '{{this}}', placeholder: 'Input with {{this}} size' },
};
{{/each}}

export const WithError: Story = {
  args: { label: 'Email', error: 'Invalid email address', defaultValue: 'invalid-email' },
};

export const WithHint: Story = {
  args: { label: 'Password', hint: 'Must be at least 8 characters long', type: 'password' },
};

export const Disabled: Story = {
  args: { label: 'Account ID', disabled: true, value: 'ACC-12345' },
};

export const Required: Story = {
  args: { label: 'Full Name', required: true, placeholder: 'Jane Doe' },
};
{{/if}}
