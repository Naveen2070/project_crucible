// Crucible preview examples for {{name}} (React Native / NativeWind).
// ponytail: Storybook for React Native isn't wired up in this release (needs Expo +
// @storybook/react-native) — these are ready-to-spread prop presets for manual/visual QA
// (e.g. `<Alert {...{{name}}Previews.info}>Message</Alert>`), not a Storybook CSF file.
import type { AlertProps } from './{{name}}';

export const {{name}}Previews: Record<string, AlertProps> = {
{{#each variants}}  {{this}}: { variant: '{{this}}', title: '{{capitalize this}}' },
{{/each}}  closeable: { variant: 'info', title: 'Dismissible', closeable: true },
};
