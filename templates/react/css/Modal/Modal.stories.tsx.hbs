import type { Meta, StoryObj } from '@storybook/react';
import React, { useState } from 'react';
import { Modal } from './Modal';
import { Button } from '../Button/Button';

const meta: Meta<typeof Modal> = {
  title: 'Crucible/Modal',
  component: Modal,
  parameters: { layout: 'centered' },
  argTypes: {
    variant:  { control: 'select', options: {{> shared/variant-options}} },
    size:     { control: 'select', options: {{> shared/size-options}} },
  },
};
export default meta;
type Story = StoryObj<typeof Modal>;

{{#if features.compoundComponents}}
export const Default: Story = {
  args: { variant: 'default', size: 'md' },
  render: (args) => {
    const [isOpen, setIsOpen] = useState(false);
    return (
      <div style=\{{ height: '300px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <button onClick={() => setIsOpen(true)}>Open Modal</button>
        <Modal {...args} isOpen={isOpen} onClose={() => setIsOpen(false)}>
          <Modal.Overlay>
            <Modal.Content>
              <Modal.Header>
                <Modal.Title>Modal Title</Modal.Title>
                <Modal.Close />
              </Modal.Header>
              <Modal.Body>
                <p>This is the modal body content.</p>
              </Modal.Body>
              <Modal.Footer>
                <div style=\{{ display: 'flex', gap: '8px', justifyContent: 'flex-end' }}>
                  <button onClick={() => setIsOpen(false)}>Cancel</button>
                  <button onClick={() => setIsOpen(false)}>Confirm</button>
                </div>
              </Modal.Footer>
            </Modal.Content>
          </Modal.Overlay>
        </Modal>
      </div>
    );
  },
};

{{#each variants}}
{{#unless (eq this "default")}}
export const {{capitalize this}}: Story = {
  args: { variant: '{{this}}' },
  render: (args) => {
    const [isOpen, setIsOpen] = useState(false);
    return (
      <div style=\{{ height: '300px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <button onClick={() => setIsOpen(true)}>Open {{capitalize this}} Modal</button>
        <Modal {...args} isOpen={isOpen} onClose={() => setIsOpen(false)}>
          <Modal.Overlay>
            <Modal.Content>
              <Modal.Header>
                <Modal.Title>{{capitalize this}} Variant</Modal.Title>
                <Modal.Close />
              </Modal.Header>
              <Modal.Body>
                <p>Modal content with {{this}} variant.</p>
              </Modal.Body>
            </Modal.Content>
          </Modal.Overlay>
        </Modal>
      </div>
    );
  },
};
{{/unless}}
{{/each}}

{{#each sizes}}
export const {{capitalize this}}: Story = {
  args: { size: '{{this}}' },
  render: (args) => {
    const [isOpen, setIsOpen] = useState(false);
    return (
      <div style=\{{ height: '300px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <button onClick={() => setIsOpen(true)}>Open {{capitalize this}} Modal</button>
        <Modal {...args} isOpen={isOpen} onClose={() => setIsOpen(false)}>
          <Modal.Overlay>
            <Modal.Content>
              <Modal.Header>
                <Modal.Title>{{capitalize this}} Size</Modal.Title>
                <Modal.Close />
              </Modal.Header>
              <Modal.Body>
                <p>Modal content with {{this}} size.</p>
              </Modal.Body>
            </Modal.Content>
          </Modal.Overlay>
        </Modal>
      </div>
    );
  },
};
{{/each}}
{{else}}
export const Default: Story = {
  args: { variant: 'default', size: 'md', title: 'Modal Title' },
  render: (args) => {
    const [isOpen, setIsOpen] = useState(false);
    return (
      <div style=\{{ height: '300px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <button onClick={() => setIsOpen(true)}>Open Modal</button>
        <Modal
          {...args}
          isOpen={isOpen}
          onClose={() => setIsOpen(false)}
        >
          <p>This is the modal content.</p>
        </Modal>
      </div>
    );
  },
};

{{#each variants}}
{{#unless (eq this "default")}}
export const {{capitalize this}}: Story = {
  args: { variant: '{{this}}', title: '{{capitalize this}} Variant' },
  render: (args) => {
    const [isOpen, setIsOpen] = useState(false);
    return (
      <div style=\{{ height: '300px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <button onClick={() => setIsOpen(true)}>Open {{capitalize this}} Modal</button>
        <Modal
          {...args}
          isOpen={isOpen}
          onClose={() => setIsOpen(false)}
        >
          <p>Modal content with {{this}} variant.</p>
        </Modal>
      </div>
    );
  },
};
{{/unless}}
{{/each}}

{{#each sizes}}
export const {{capitalize this}}: Story = {
  args: { size: '{{this}}', title: '{{capitalize this}} Size' },
  render: (args) => {
    const [isOpen, setIsOpen] = useState(false);
    return (
      <div style=\{{ height: '300px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <button onClick={() => setIsOpen(true)}>Open {{capitalize this}} Modal</button>
        <Modal
          {...args}
          isOpen={isOpen}
          onClose={() => setIsOpen(false)}
        >
          <p>Modal content with {{this}} size.</p>
        </Modal>
      </div>
    );
  },
};
{{/each}}
{{/if}}
