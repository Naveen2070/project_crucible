// Crucible preview examples for {{name}} (React Native / StyleSheet).
// ponytail: Storybook for React Native isn't wired up in this release (needs Expo +
// @storybook/react-native) — these are ready-to-spread prop presets for manual/visual QA
// (e.g. `<Card {...{{name}}Previews.default}>Card content</Card>`), not a Storybook CSF file.
import type { CardProps } from './{{name}}';

export const {{name}}Previews: Record<string, Omit<CardProps, 'children'>> = {
{{#each variants}}  {{this}}: { variant: '{{this}}', title: '{{capitalize this}} Card' },
{{/each}}{{#each sizes}}  {{this}}Size: { size: '{{this}}', title: '{{capitalize this}} Size' },
{{/each}}
};
